# --- 웹 프레임워크 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- DB ---
SQLAlchemy==2.0.36

# --- HTTP 클라이언트 (OpenDART / 공공데이터포털 비동기 호출) ---
httpx==0.28.1

# --- 설정 관리 ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- 재무제표 원문 파싱 ---
lxml==5.3.0
pdfplumber==0.11.4
olefile==0.47          # HWP(OLE2 컨테이너) 파싱용

# --- 결과 산출 (Excel/CSV) ---
openpyxl==3.1.5
pandas==2.2.3

# --- 테스트 ---
pytest==8.3.4
pytest-asyncio==0.25.0
