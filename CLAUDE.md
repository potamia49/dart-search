# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 하네스: dart-search 개발팀

**목표:** 백엔드/파이프라인/DB(dart-backend), 감사보고서 원문 파싱(dart-parser),
프론트엔드(dart-frontend) 전문 에이전트와 읽기 전용 리뷰 에이전트(dart-qa,
dart-design-review)를 조율해 구현 → 검증까지 일관되게 수행한다.

**트리거:** 백엔드/파이프라인/DB/파서/프론트엔드 구현 작업이나 QA·디자인 리뷰 요청 시
`dart-search-team` 스킬을 사용하라(`.claude/skills/dart-search-team/SKILL.md`). 단순
질문이나 문서 조회는 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-05 | **템플릿 예외 수정 5건째(⑤)** — 비교군이 없을 때(`regionGroup=[]`) "동일 지역·규모 비교군 순위" 차트와 "비교군 상세 현황" 표 자리에 안내 문구를 보이도록 `tamplate/audit_proposal_template.html`을 **HTML 2줄(안내 div) + CSS 4줄**만 고침(`#regionTable tbody:empty`를 조건으로 쓰는 `:has()` 규칙, **JS 무변경**). 함께 L2/L3 **문서화만**(코드 무변경) — `select_region_group()`/`match_by_industry_prefix()` docstring에 단서 추가. 테스트 2건 추가(463 → **465 passed**) | dart-backend | dart-qa 리뷰(로직 버그 0건, peers 1,488건 전수 무회귀 확인) 후속. 바로 위 업종 매칭으로 `regionGroup=[]`이 63건(4.2%, Job별 최대 20.2%) 생겼는데, regionGroup이 비면 peers도 반드시 비어(지역 후보가 peers 후보의 상위집합) **2페이지 비교 영역 3개가 통째로 백지**로 인쇄됐다. peers 쪽은 이미 각주("동종업종 비교사 데이터 없음")가 있는데 지역 쪽 두 섹션만 조용했다 — 값이 정상(빈 목록이 맞는 답)이라 백엔드로는 고칠 수 없어 사용자 승인 하에 템플릿을 고쳤다 |
| 2026-08-05 | **지역 비교군(`regionGroup`)에 업종 매칭 추가** — peers 안에 인라인으로 있던 소분류(3자리)→중분류(2자리) 1회 폴백 로직을 `match_by_industry_prefix()`로 추출해 **두 비교군이 같은 함수를 호출**하게 하고, `select_region_candidates()`가 ①자기 자신 제외 → ②**업종 매칭** → ③최소 표본(2건) → ④매출 근접순 상한 절단 순으로 거르도록 변경. 기존 regionGroup 규칙(휴면·폐업 추정 제외, ±100% 비율 상한, 의견거절/부적정 수치 감춤, "미상" 표기, 대상 첫머리 `isTarget`, 상한 16건)은 **전부 무변경**이고 `peers`/`industryAverage`도 무영향. 테스트 6건 추가 + 기존 4건 갱신(457 → **463 passed**), **템플릿 무수정** | dart-backend | 사용자가 실제 생성된 문서의 "비교군 상세 현황" 표에서 조선부품·금속구조재·단조·시내버스운송·화학제품·고무제품·플라스틱제품·일반목적기계·도장공사·상품중개가 **한 장에 나란히** 실린 것을 보고 "업종기준으로 필터해야지"라고 직접 지적. 지금까지의 설계는 "Job의 검색 조건이 곧 지역이므로 Job 전체가 같은 지역"이라는 근거로 업종을 보지 않았는데, 그러면 업종이 제각각인 회사끼리 매출총이익률 순위를 매겨 인쇄하게 돼 비교의 의미가 없다. 개발 DB 실측(Job 25/26/27, 후보 1,488건): 지역 표의 비교사가 평균 15.00 → 8.37/4.09/11.95건(Job별)으로 줄고, 소분류 매칭 1,200건 / 중분류 폴백 225건, 동종업종 부족으로 표가 통째로 비는 회사가 63건(4.2%) 새로 생긴다 |
| 2026-08-04 | **템플릿 예외 수정 4건째(F1)** — `tamplate/audit_proposal_template.html`의 `.opinion-tag` 기본 규칙에 `background:var(--gray);` **1줄만** 추가(전용 클래스 3종은 specificity로 덮어써 색 무변경) + 회귀 테스트 1건(456 → **457 passed**) | dart-backend | dart-qa F1. 기본 규칙에 배경 없이 `color:#fff`만 있어 `적정`/`한정`/`의견거절` 외의 값 — 기존부터 있던 `부적정`과 방금 L5로 새로 채워지는 `미상` — 이 **흰 종이 위 흰 글자**로 인쇄돼 지역 비교표에서 사실상 보이지 않았다(개발 DB 실측 143건, 4.7%). 앞선 예외 ①②③과 달리 자본잠식과 무관한 순수 CSS 결함이고, 백엔드 데이터로는 고칠 수 없어(값 자체는 정상) 사용자 승인 하에 템플릿을 고쳤다 |
| 2026-08-04 | 보고서 비교군 잔여 지적 5건(L1~L5) 정리 — L1 peers 각주(`industryAverage.설명`)에 기준 연도 표기(`build_comparison_year_note()`), L2 비교군 후보의 PARTIAL 연도를 대상 회사당 한 줄 경고로(`PeerCandidate.partial`/`collect_comparison_warnings()`), L3 `compute_report_ratios()` 분모 가드를 truthy 검사 → 명시적 `<= 0` 비교로 강화, L4 업종코드 자릿수 비대칭은 **문서화만**(코드 무변경), L5 감사의견 공란을 지역 표에서 `"미상"`으로. 테스트 8건 추가(448 → **456 passed**), **템플릿 무수정** | dart-backend | dart-qa 1차 리뷰에서 나왔으나 우선순위가 낮아 미뤄 둔 5건. L1은 후보의 재무값이 각 회사 자신의 최근 연도라 실측 peer 짝 9,753건 중 602건(6.2%)이 대상과 연도가 다른데 문서에 아무 표기가 없어 "동일 시점 비교"로 오독됐다. L2는 대상 회사 자신의 PARTIAL만 경고하고 비교군 쪽(실측 후보 8건 → 149개 문서)은 조용했다. L3은 `not x` 검사가 음수를 통과시켜 이 저장소가 자본잠식으로 세 번 겪은 버그가 다른 호출부에서 재발할 수 있는 상태였다. L5는 감사의견 공란 후보 5건이 76개 문서에서 **글자 없는 빈 태그**로 인쇄됐다 |
| 2026-08-04 | 보고서 비교군 후속 보정 3건 — ① 의견거절·부적정 회사를 peers 후보에서 제외 + regionGroup은 금액/비율만 null(한정은 그대로), ② 매출총이익률·영업이익률 ±100% 밖 후보를 `build_peer_pool()`에서 제외(`MAX_COMPARISON_ABS_RATIO`), ③ `industryAverage.설명` 문구를 "매출 규모가 유사한 동종업종 N개사"로 수정(계산 무변경). 회귀 테스트 3건 + 문구 검증 갱신(445 → 448 passed) | dart-backend | dart-qa 리뷰(설계 규칙 10개는 정확히 구현·로직 버그 없음 확인) 후 **우편 발송 문서 관점**으로 사용자가 확정한 3건. ①은 템플릿 규약(250-252행)이 regionGroup에서만 실제로 지켜지고 peers는 `chartPeer`가 null 체크 없이 곱해 `null*100 == 0` → "0.0%"라는 **틀린 값이 조용히** 찍히기 때문(제외가 아니라 왜곡). ②는 실측 `(주)근하하이테크산업`(매출총이익률 -1857.9%, `parse_status=OK`) 한 건이 지역 순위 차트 축을 끌고 가 대상 회사 막대가 7px로 뭉개졌기 때문. ③은 상한 8건 절단 후 표본이라 "동일 업종 8개사"가 "그 업종에 8개사뿐"으로 오독되기 때문 |
| 2026-07-21 | 하네스 최초 등록 — 오케스트레이터 스킬(`dart-search-team`) 신설 + 기존 5개 에이전트(dart-backend/frontend/parser/qa/design-review) 내용을 M8 최신 상태로 갱신 | 전체 | 에이전트 정의 파일만 존재하고 오케스트레이터·CLAUDE.md 등록이 없던 구조적 누락 발견. 에이전트 내용도 M1~M5 스캐폴딩 단계에 머물러 M8까지의 아키텍처 재설계(지역 필터가 금융위 API 사전 스크리닝→dart_corp_index 로컬 쿼리로 전환, Phase1/Phase2 분리, 참고값/확정치 분리 등)를 전혀 반영하지 못하는 drift를 감사로 확인 |
| 2026-08-03 | "현재 필터 전체 선택"용 경량 id 조회 — `GET /api/jobs/{id}/results`에 `ids_only=true` 옵션 추가(`ResultListResponse.ids` 필드 신설, 새 엔드포인트·스키마 변경 없음) | dart-backend | 상단 "필터 전체 wide 내보내기"를 없애고 선택 항목 다운로드(long) 하나로 통합하기로 하면서, 페이지당 50건만 고를 수 있던 제약을 풀어야 했다. 기존 목록 조회 재사용은 `page_size` 상한 500이 **조용히** 적용돼 "전체 선택인데 500건만"이 되고, `ResultResponse` 63필드(실측 약 1.9KB/행)를 id 하나 쓰자고 수 MB 받아야 해 기각 |
| 2026-08-03 | 결과 화면 다운로드 UI 정리 — 상단 "Excel 다운로드"/"CSV 다운로드" 버튼(필터 전체 wide 내보내기) 제거 + 선택 표시줄에 "현재 필터 전체 선택 (총 N건)" 버튼 추가(`handleSelectAllInFilter`), 선택 0건이어도 표시줄 유지(비활성 버튼 + 안내문구로 유일한 다운로드 입구임을 알림) | dart-frontend | 사용자가 "상단 버튼과 선택 항목 다운로드가 중복 아니냐"고 문제 제기 → 헤더 체크박스가 페이지당 50건만 선택돼 "전체 선택"이 비현실적이었던 게 진짜 원인으로 확인 → 위 `ids_only` 전제 위에 필터 전체 선택 기능을 추가해 선택 항목 다운로드(long 포맷) 하나로 통합, wide 포맷(당기+전기)은 사용자가 "전기 금액 불필요" 확인해 화면에서 제외 |
| 2026-08-04 | 보고서 비교군 3필드(`peers`/`industryAverage`/`regionGroup`)를 **같은 Job의 다른 results**로 채움 — `app/reports/audit_proposal.py`에 비교군 섹션 신설(`PeerPool`/`build_peer_pool`/`select_peers`/`build_industry_average`/`select_region_group`/`compute_report_ratios`) + `build_report_payload()`/`generate_reports()`에 `peer_pool` 인자 추가 + `app/api/results.py::_load_job_peer_pool()`(요청당 벌크 조회 1회) 배선, 테스트 14건(전체 431 → 445 passed) | dart-backend | 템플릿에 이미 있던 동종업종/지역 비교 차트·표·업종평균 가점이 데이터 소스가 없어 통째로 비어 인쇄되고 있었다. 신규 API 호출·스키마 변경 0건으로 같은 Job에서 이미 수집한 데이터만 쓴다. 부수 효과로 템플릿 `scoreProfitability()`의 업종평균 항이 살아나 수익성 등급이 상대평가로 바뀐다(실측 1,522건 중 550건 이동) |
| 2026-08-04 | `selection-summary` 필드 보정 — `failed`를 화면 "파싱 실패(검수 필요)" 탭과 동일한 조건(`parse_status='FAILED'` **AND** `rcept_no IS NOT NULL`)으로 좁히고 `no_disclosure`(감사보고서 없음) 분리 + `no_history`(재무이력 0건 → 생성 스킵) 신설, 회귀 테스트 2건(전체 431 passed) | dart-backend | dart-qa 실측. ① Phase 2 B1이 공시 미발견 건에도 `parse_status=FAILED`를 쓰기 때문에 FAILED 2,215건 중 2,214건(99.95%)이 `rcept_no IS NULL`이고 진짜 검수 필요는 1건인데 확인 모달은 "파싱 실패 1,871건"이라고 띄우고 있었다. ② `generate_reports()`가 재무이력 0건 회사를 건너뛰는데(실측 Job 27: 4,383건 중 3,149건) 모달은 "4,383건 생성"이라고 물어 실제 산출량(최대 1,234건)을 과장했다 |
| 2026-08-03 | 보고서 생성 전 확인 모달의 전제 — 선택 id 목록 요약 집계 `POST /api/jobs/{id}/results/selection-summary` 신설(+ 타 Job id 400 검증을 `_assert_ids_belong_to_job()`로 공용화, tests 3건) | dart-backend | "현재 필터 전체 선택"(§4-11-A)으로 수천 건이 선택된 채 확인 없이 보고서가 생성되고, 탭을 넘나든 합집합 선택에 조건 불일치 회사(휴면·폐업 추정/매출액·총자산 제외)가 섞여 우편 발송될 수 있다는 dart-qa·dart-design-review 지적 |
| 2026-08-03 | 선택 항목 보고서 생성 기능 백엔드 신설 — `POST /api/jobs/{id}/generate-report` + `app/reports/`(audit_proposal.py/firm_profile.py) + `Settings.report_output_dir`/`report_template_path` + tests/test_reports.py(23건) | dart-backend | 선택 회사별 감사 수임 제안서(HTML)를 로컬 폴더로 뽑아 우편 발송까지 하려는 신규 요구. 기존 "선택 항목 다운로드"(브라우저 다운로드)를 대체하지 않고 병행한다 |
| 2026-08-03 | 보고서 생성 QA 지적 반영 — 재무연도 선별(`select_financial_rows`) 신설로 "쓸 수 있는 연도 0건이면 생성 스킵(`skipped[]`)", 결측/0분모 연도 제외, FAILED 제외·PARTIAL/전기유래 경고, `FIRM_PROFILE` 자리표시자 경고, 라벨 엑셀 제어문자 방어(507 통일) + tests/test_reports.py 34건 | dart-backend | dart-qa 리뷰 High 3/Medium 2. 특히 빈 `financials`가 템플릿 렌더 IIFE를 `TypeError`로 중단시켜 **연락처까지 빠진 반쪽 제안서가 우편 발송**될 수 있었고, 결측치가 `NaN%`로 인쇄되며 등급 산정이 "데이터가 없을수록 A"로 고장나는 것을 확인 |
| 2026-08-03 | **템플릿(`tamplate/audit_proposal_template.html`) 예외 수정** — `scoreStability()` 맨 앞에 자본잠식 가드(`if(last.부채비율 < 0) return 10;`) 1줄 추가 + 회귀 테스트 2건(tests/test_reports.py, 415→417 passed) | dart-backend | dart-qa 2차 리뷰 H4. 완전자본잠식(자본총계<0) 회사는 부채비율이 **음수**라 `< 0.5` 임계값 비교에서 +30점을 받아 **재무안정성 B~A(양호·우수)**로 인쇄됐다(개발 DB 실측: 해당 128건 **전부** B 이상). 음수는 null/0/NaN/Infinity가 아니라 `select_financial_rows()`의 결측·0분모 가드를 정상값으로 통과해 **백엔드로는 막을 수 없어**, 사용자 승인 하에 "템플릿은 수정하지 않는다" 관행의 예외로 처리 |
| 2026-08-03 | **템플릿 예외 수정 2건째** — `buildRiskList()`의 부채비율 "개선" 판정에 자본잠식 가드 추가(`last.부채비율 < 0`이면 red "완전자본잠식" 항목, 개선은 `first.부채비율 >= 0`일 때만) + 회귀 테스트 2건(tests/test_reports.py 36→38, 전체 422 passed) | dart-backend | 바로 위 H4와 같은 뿌리 — 음수 부채비율이 `last < first` 비교도 통과해 **자본잠식으로 악화된 회사에 "지속 개선되어 재무안정성이 강화되었습니다" 초록 문구**가 인쇄됐다(실사례 `results.id=6223` 903.1%→-336.8%, 실측 81건). 같은 문서의 등급은 D(주의 필요)라 **한 장 안에서 서술이 모순**돼 사용자가 "함께 수정"으로 확정 |
| 2026-08-03 | **템플릿 예외 수정 3건째 + 백엔드 가드 확장** — `renderDoughnut()` 음수 조각 시 구성비 대신 금액 안내로 대체 / `renderLineChart()` y축을 음수까지 포함(`minV`/`yOf`/0선) / 백엔드 `RATIO_POSITIVE_DENOMINATOR_KEYS` 신설로 매출액·자산총계·유동부채는 **음수 연도도 제외**(자본총계는 제외 안 함) + 회귀 테스트 6건(tests/test_reports.py 39→45, 전체 422→429 passed) | dart-backend | dart-qa 3차 리뷰 Medium 3건. 등급·리스크 문구는 이미 자본잠식을 반영하는데 **차트만 모순**됐다 — 도넛은 "유동부채 903.2% … 자본총계 -864.7%"를 인쇄했고(실측 8583), 선그래프는 음수 부채비율 4개 점이 plot 밖으로 잘려 통째로 안 보였다. 매출액이 음수인 연도(실측 6704, -5,409백만원)는 영업이익률이 +31.5%로 뒤집혀 **수익성 A**가 찍혔다. 자본총계를 같은 `<= 0` 가드에 넣었다면 전 연도 자본잠식 회사 66곳이 보고서 미생성으로 바뀌어 H4 방침과 충돌해 항목별로 다르게 처리 |
| 2026-07-22 | 구 파이프라인 죽은 코드 물리 삭제 — `run_job()`(구 STEP 1~7 오케스트레이터), STEP 3 지역/업종 필터(`_run_region_industry_filter` + 하위 헬퍼), `CorpProfile` 모델/`corp_profiles` 정의, A3 `DartClient.get_summary_financial_stat()`, 관련 test_pipeline.py 18건 제거 | dart-backend | "M8에서 A3/A4가 제거됐다"는 기존 서술이 실제로는 **호출 경로만 끊기고 코드는 파일에 남아 있던** 상태였음이 dart-qa 2회 정밀 조사로 드러남(프로덕션 import 0건 확정). 죽은 코드를 실제 소스에서 삭제해 확정. 살아있는 `list_summary_financial_stats`/`FscCorpInfoClient`/Phase 2 B1 폴백(`_resolve_alternative_corp_code`/`CorpCache`)은 무변경 |

## 프로젝트 현황

> **이 섹션은 요약이다.** 세션마다 쌓인 상세 조사·실측·버그 수정 경위(왜 이렇게
> 됐는지)는 전부 [개발이력.md](개발이력.md)에 시간순으로 보존돼 있다. 여기는
> "지금 무엇이 참인가"만 담는다 — 새 세션에서 상태가 바뀌면 여기부터 갱신하고,
> 경위는 개발이력.md 맨 아래에 이어서 적을 것.

**마일스톤 M1~M8 전체 완료(2026-07-21 기준).** 스캐폴딩(M1) → 수집 파이프라인
+ 재무제표 파싱(M2/M3) → 프론트엔드(M4) → 실전 검수(M5) → 아키텍처 재설계
설계·구현(M6/M7) → 재설계 파이프라인 전환 및 정합성 검증(M8)까지 마쳤다.
제품 범위는 **"데이터 수집기 + 결과 조회 웹앱"으로 확정**돼 있다 — 전단지/
진단자료 생성 등 활용 단계(구상 당시 "Phase 2"라 불렀던 것)는 범위에서
완전히 제외했다(2026-07-17).

### 현재 아키텍처 (M8 재설계 후 최종 상태)

Job은 `phase` 컬럼(`CANDIDATES`/`FINANCIALS`)으로 2단계로 나뉜다 —
`POST /api/jobs`는 후보 확정(Phase 1)까지만 실행하고 멈추며, 사용자가 후보
목록을 검토한 뒤 `POST /api/jobs/{id}/start-financials`를 명시적으로 호출해야
실제 DART 원문 크롤링(Phase 2)이 시작된다.

- **Phase 1(후보 확정)은 외부 API 호출이 0건이다.** `dart_corp_index`(DART
  corpCode 전수 인덱스, `corp_code`가 PK) 로컬 DB 쿼리만으로 지역/업종/상장
  여부를 확정한다. 주소·대표자·업종명은 DART 정본 데이터라 그 자리에서 확정치다.
- **금융위(FSC) API 기반의 두 메커니즘은 모두 제거됐다**:
  - "A3"(건별 재무 사전 스크리닝, `GetFinaStatInfoService_V2`로 매출액/총자산을
    미리 걸러 다운로드 대상을 줄이던 최적화)는 스냅샷이 최대 1년 묵어 있어
    조건에 맞는 회사의 **25.3%를 조용히 누락**시켰다 — 폐기.
  - "A4"(이름 매칭으로 corp_code를 추정하던 폴백)는 동명이인 corp_code
    오매칭(실측 11.6~12.7%)의 근본 원인이었다 — `dart_corp_index`가
    `corp_code` 자체를 PK로 가지므로 이름 매칭 자체가 불필요해져 제거됐다.
  - **주의(2026-07-22):** M8 3단계 당시의 위 "제거"는 실제로는 **호출 경로만
    끊고 코드는 파일에 남아 있던** 상태였다(구 오케스트레이터 `run_job()`(STEP
    1~7)이 프로덕션에서 import되지 않는 죽은 코드였고, A3 메서드
    `DartClient.get_summary_financial_stat()`·STEP 3 지역/업종 필터
    `_run_region_industry_filter()`·`CorpProfile` 모델도 그 죽은 경로에서만
    참조됐다). dart-qa 2회 정밀 조사로 프로덕션 호출 0건을 확정한 뒤
    2026-07-22에 이들을 **실제 소스에서 물리 삭제**했다(테스트 18건 포함) —
    "설계상 제거"가 이제 "코드에서도 부재"로 확정됐다. `list_summary_financial_stats`
    (이름이 비슷하나 `crno` 없이 연도 단위 전수 페이징하는 `ref_*` 적재용 별개
    메서드)·`FscCorpInfoClient`·Phase 2 B1 폴백(`_resolve_alternative_corp_code`/
    `CorpCache`)은 **살아있는 코드라 무변경**. 기존 DB의 `corp_profiles` 물리
    테이블은 무해하게 남겨 뒀다(모델 삭제로 이후 생성 안 됨).
- **매출액/총자산 참고값**(`fsc_financial_stat` 테이블에서 가져온
  `results.ref_revenue`/`ref_total_assets`/`ref_fin_year`)은 **오직 Phase 2
  처리 순서 결정(조건 밴드 근접도順 정렬)에만 쓰이고, 후보를 제외하는 데는
  절대 쓰이지 않는다.** 최종 포함/제외 판정은 항상 B4(Phase 2, 실제 감사보고서
  원문을 파싱한 뒤의 사후 필터) 한 곳에서만 이뤄진다 — `results._cur`/`_prv`
  (확정치)와 `ref_*`(참고치)는 컬럼 자체가 분리돼 있어 섞일 위험이 없다.
- **`fsc_corp_index`(구 대응 1의 산물, data.go.kr `getCorpOutline_V2` 전수
  크롤, 633,968행)는 실전 3개 지역 완주 + 오매칭 0 검증 후 삭제 완료
  (2026-07-21)**. `fsc_financial_stat`(참고값 스냅샷)은 이름이 비슷하지만
  별개 테이블이며 계속 사용 중이다 — 혼동 주의.
- **업종 필터는 DART 자체 업종 트리(대분류 21 / 중분류 77 / 소분류 234)**를
  쓴다. 세분류·세세분류는 회사별 분류 깊이 편차로 prefix 매칭에서 조용한
  누락(20.9%/41.3%)이 발생해 화면에 노출하지 않는다.
- **동명 회사 위치 결합(merge) 정합성**: `dart_corp_index`를 갱신하는
  `merge_by_position()`은 회사명만으로 정합성을 검사해 동명 회사끼리 자리가
  바뀌면 주소·업종이 조용히 교차될 수 있다 — `reconcile_ambiguous_rows()`가
  위험 그룹만 DART 정본으로 재대조하며, `dart-index/refresh`가 크롤 완료 시
  자동으로 이어서 실행한다(끄려면 `reconcile: false`). 상태는
  `GET /api/meta/dart-index/status`의 `reconcile_pending`으로 확인 가능.

### 파서(dart-parser) 핵심 사실

> 아래는 "지금 무엇이 참인가"만 담은 요약이다. 각 항목의 실측 수치·정규식
> 설계·회귀 fixtures 등 상세 경위는 전부 [개발이력.md](개발이력.md)에 날짜순으로
> 있다.

- 원문은 **XML이 절대다수**(HWP 미구현, PDF는 best-effort). XML 선언부의
  인코딩 표기를 신뢰하지 말 것 — 실측 약 4.4%가 선언과 달리 EUC-KR/CP949였다.
  `_decode_raw_xml()`이 UTF-8 실패 시 CP949로 자동 폴백한다.
- **계정과목 라벨 표기가 회사마다 크게 다르다**(로마숫자 유니코드/아스키/유사
  문자 혼용, 셀 안 개행, 각주·산식·EPS 병기 접미어 등). 새로운 "라벨이 안
  잡힌다" 계열 버그를 다룰 때는 fixtures 표본이 아니라 **로컬 문서 캐시
  전체(API 호출 0건으로 스캔 가능, 현재 약 4,900건)를 대상으로 근접 불일치를
  찾는 스크립트**를 먼저 돌리는 것이 개별 사례 추적보다 훨씬 효율적임을
  확인했다. 이 관행으로 발견·수정된 라벨 매칭 버그들(각주 참조의 한글 접속사,
  연결당기순이익, EPS 병기 접미어, "당기순손익" 등 요약행 변형 5종, IFRS
  첨부 서식의 값 없는 소계 헤더 등)은 전부 개발이력.md에 기록돼 있다
  (2026-07-21~23).
- **"손실" 라벨 부호 처리**(`xml_parser.py::_apply_sign`): 판정은 반드시
  `normalize_account_label`로 공백·개행을 제거한 라벨로 한다(alias 조회와
  동일 기준). 규칙은 "정규화 라벨에서 **먼저 나오는** 이익/손실 키워드가 주
  계정" — 순수 손실은 항상 반전, 이익-primary 조합형("영업이익(손실)")은
  원문 부호 신뢰, **손실-primary 조합형**("매출총손실(이익)")은 경제적 부호가
  반대라 반드시 반전한다. 실측 버그 2종(라벨 글자 사이 공백, 손실-primary
  오처리)은 회귀 테스트로 잠갔다.
- **매출총이익은 비율이 아니라 금액**(`gross_profit_cur/prv`)으로 원문에서
  직접 파싱한다(2026-07-20, 기존 `gross_margin`(%) 계산값 폐기).
- **IFRS "(첨부)재무제표" 첨부문서 구조 지원 완료**(2026-07-22). "재무상태표"/
  "손익계산서"/"현금흐름표" TITLE 없이 "(첨부)재무제표" 하나 아래 4개 재무제표가
  모두 들어가는 서식 — 별도 부호 규약(`_apply_sign_ifrs`, FINANCE와 정반대라
  손익은 원문 부호 신뢰·cogs/sga만 abs)과 별도 추출 경로
  (`_extract_attach_section`)로 처리한다. **원문 섹션 열람(`document_sections.py`)
  과 세부계정 펼치기(`account_detail.py`)도 이 서식을 인식**하도록
  2026-07-27에 공용 워커(`xml_parser.walk_statement_tables`)로 통합돼, 세 모듈이
  서식 인식 기준을 공유한다(전에는 파이프라인만 지원해 UI 두 곳이 "원문을
  찾을 수 없음"으로 표시되는 drift가 있었다).
- **IFRS "(첨부)재무제표" 서식 중 열 제목이 "당기"/"전기"가 아니라 "제N기
  기말"/"제N기"(회계 차수)뿐인 변형도 인식**(2026-07-28). 로컬 캐시 8,748건
  전수 스캔 기준 462건 중 63건이 해당 — `_build_period_column_plan()`이 차수
  헤더를 **등장 순서**(숫자 크기 아님)로 당기/전기 폴백하되, 한쪽만 "(당)"/
  "(전)" 명시가 있으면 그 명시를 우선하고 양쪽이 같은 역할로 충돌할 때만
  순서로 되돌아간다(dart-qa 리뷰로 비대칭 명시 케이스의 잠재적 오판정을
  발견해 보완). 중단영업 분리 서식(net_income 합계 행 없음)은 별도 한계로
  남겨둠 — 상세 경위는 개발이력.md 참고.
- **현금흐름표 4항목(CF) + 세부계정 펼치기, 감사인명/사무소주소
  (`auditor_name`/`auditor_address`), 원문 섹션 열람 API**(재무상태표/
  손익계산서/현금흐름표/주석/감사의견 — 로컬 캐시만 읽어 쿼터 0건)가 모두
  구현돼 있다.
- **영업외수익/영업외비용 2항목**(`non_operating_income`/`non_operating_expense`,
  2026-07-22)이 CF 4항목과 동형인 best-effort 필드로 구현돼 있다 — 파서/DB
  컬럼/파이프라인 매핑/API 응답/Excel·CSV 내보내기 전부 배선 완료.
  `determine_parse_status()` 판정에는 관여하지 않는다(결측이어도 PARTIAL/
  FAILED로 안 떨어짐).
- **연도별 감사인 추출 + "감사인 변동" tri-state 플래그**(`financial_snapshots.
  auditor_name` / `results.auditor_changed` 1/0/**NULL**, 2026-07-26)가
  백엔드·프론트 모두 구현 완료. 비교는 `_auditor_key()`(pipeline.py)로 표기
  차이(공백/접미어 순서/서명자 딸림)를 흡수한 뒤 한다 — dart-qa 리뷰로 이
  정규화 자체의 버그 1종과 파서 추출 단계의 별개 버그 1종을 발견해 수정했고
  (**[보정 1]/[보정 3]**), 연도별 변경 판정은 DB에 저장하지 않고 서버가 이력
  조회 시 순수 함수로 계산해 내려준다(**[보정 2]** — 프론트 자체 비교 로직
  제거).
- **`financial_snapshots`의 재무상태표/손익계산서/CF/영업외손익 값 자체가
  구버전 파서 결과에 고정돼 있던 문제를 발견해 소급 재파싱으로 해결**
  (2026-07-26/27). STEP7이 최초 파싱 시점의 결과를 영구 보관해 이후 파서
  개선이 자동 반영되지 않던 구조적 문제였다 — `results`는 여러 차례 소급
  반영됐지만 `financial_snapshots`는 그 대상에서 빠져 있었다.
- **스키마 확장은 항상 "컬럼 추가 + 소급 재파싱 없음"** 패턴을 따른다(신규
  Phase 2 실행분부터만 채워짐). 지금까지 사용자가 명시적으로 승인한 소급
  재파싱이 여러 차례 일회성으로 실행됐다(스크립트: `backend/scripts/
  reparse_local_cache.py`(results 전반, `--dry-run`/`--verify` 지원) /
  `reparse_financial_snapshot_values.py`(snapshots 값 전체) /
  `backfill_auditor_names.py` / `backfill_stale_disclosure.py` 등 — 각
  실행의 대상 건수·검증 결과는 개발이력.md 참고). 잔여 미해결: 상세열 "-"를
  명시적 0으로 오파싱해 cogs=0이 되는 별개 버그 2건(20230410002954/
  20230406001585) — gross_profit 부호는 정확, 부호 이슈 아님(향후 과제).

### 결과 조회 화면

`parse_status`(OK/PARTIAL/FAILED) 필터 + "감사보고서 없음"(`rcept_no`
NULL, 검수 불필요)과 "파싱 실패"(검수 필요)를 구분 표시, 컬럼 정렬/검색,
선택 항목 다운로드(개별 체크 또는 "현재 필터 전체 선택" 후, 아래 §4-11/§4-11-A
참고), 재무 이력(최근 N년) 표 + 원문 섹션 열람 모달, 후보 목록 화면에서
개별 후보 선택 제외(Phase 2 시작 전).
"감사인 변동 여부" 컬럼(목록)과 상세 재무이력의 연도별 감사인 표시·강조는
**2026-07-26 구현 완료**(위 "연도별 감사인" 항목 참고).

**사후 필터 제외 건 기본 숨김(2026-07-28 확장)**: "휴면·폐업 추정"
(`excluded_by_stale_disclosure`, 2026-07-22)과 같은 패턴으로
`excluded_by_revenue`/`excluded_by_assets`(매출액·총자산 조건 제외 건)도
전용 탭(매출액 제외/총자산 제외) 외 모든 탭에서 기본적으로 숨긴다 — 검토
계기: 총자산 조건에 안 맞아 제외된 회사가 "전체" 탭에 그대로 노출돼
사용자가 재무 이력 부재를 오인한 사례. 한 회사가 매출액·총자산 두 조건에
동시에 걸릴 수 있어(두 필터는 독립 판정) `tabToParams()`는 baseline
스프레드 대신 STALE_DISCLOSURE와 같은 조기 반환 방식을 쓴다(안 그러면
반대쪽 필드가 `false`로 남아 두 탭 어디에도 안 뜨는 사각지대가 생김).
숨김 건수 안내는 ALL 탭에서만 노출한다(`excluded_by_revenue=1`이 서려면
원문 파싱이 선행돼야 해 FAILED/NO_DISCLOSURE 등 검수 탭에서는 실제 숨김이
0건에 가까워 안내가 오해를 유발하기 때문 — `staleCount`는 이 제약이 없어
세부 탭 안내를 유지). 필터 전체 내보내기(`ids` 없는 export 경로, 아래 참고)도
화면 필터를 그대로 따라가 이 숨김이 다운로드 파일에도 적용된다(기존 stale과
동일 원칙, 사용자 확인 완료) — 선택 항목 다운로드(`ids` 지정)는 필터 무관이라
영향 없음.

**다중 선택 다운로드(§4-11/M9, 백엔드·프론트 모두 2026-07-28 완료)**: 목록 왼쪽
체크박스로 회사를 골라 그 회사들만 별도 파일로 받는다 — 도입 당시(2026-07-28)엔
기존 "전체 내보내기"를 **대체하지 않고 병행**했으나, 2026-08-03에 §4-11-A("현재
필터 전체 선택")가 생기면서 화면의 유일한 다운로드 경로가 됐다(아래 §4-11-A
참고 — 상단 [Excel/CSV 다운로드] 버튼은 제거됐고, `ids` 없는 export API 자체는
남아 있다). `GET /api/jobs/{id}/export`의 `ids`(쉼표구분
`results.id`) + `include_history`(=true면 `financial_history` 시트 추가,
xlsx 전용)를 쓴다. DB 스키마 변경·추가 API 호출 0건인 순수 조회 파라미터
확장이라 기존 완료 Job에서도 즉시 쓸 수 있다. 상세 설계(정렬 규칙, 입력값
검증, 프론트 선택 상태 관리, dart-design-review 반영 내역)는
[개발이력.md](개발이력.md) 참고.

**"현재 필터 전체 선택"(§4-11-A, 백엔드·프론트 모두 2026-08-03 완료)**: 위 선택
다운로드가 페이지당 50건씩만 고를 수 있던 제약(1,000건대 Job은 수십 번 페이지를
넘겨야 함)을 풀기 위한 기능. 백엔드는 **경량 조회 옵션**을 추가했다 —
`GET /api/jobs/{id}/results?ids_only=true`를 주면 **페이징 없이** 같은 필터·정렬을
통과한 `results.id` 전체를 응답의 `ids`에 담아 준다(`items`는 빈 배열, `page`=1,
`page_size`=`total`=`len(ids)`). 새 엔드포인트를 만들지 않고 기존 목록 조회에
불리언 하나를 얹은 형태이며, DB 스키마 변경·추가 API 호출 0건이다.

- **기존 목록 조회를 `page_size=total`로 재호출하는 방식은 쓸 수 없다** —
  `page_size`는 `min(page_size, 500)`으로 **조용히** 깎이므로 1,222건을 요청해도
  에러 없이 500건만 돌아와 "전체 선택인데 일부만"이 된다. 게다가 `ResultResponse`는
  필드 63개(실측 약 1.9KB/행)라 id 하나 쓰자고 수 MB를 받게 된다. `ids_only`는
  SQL에서 `results.id` 한 컬럼만 SELECT한다(실측: 2,019건 78ms / 12.4KB).
- **`ids`는 `ids_only=true`일 때만 채워지고 그 외에는 `null`이다**(빈 배열이 아니다).
  구버전 백엔드는 모르는 쿼리 파라미터를 조용히 무시하고 1페이지만 주므로, 프론트는
  `ids == null`을 "전체 선택 미지원"으로 판정해야 한다(`/export`의 `_selected`
  파일명 접미어와 같은 fail-safe 계약).
- 전체 선택 후 `/export?ids=`로 넘길 때의 URL 길이는 실측상 안전하다 — httptools
  기준 64KB까지 정상(160KB에서 400), id 1건이 인코딩 포함 약 7~8B라 최대 Job
  전량(4,383건)이 약 32KB다.
- **프론트엔드**: 결과 화면 선택 표시줄(`ResultPage.tsx`)에 "현재 필터 전체 선택
  (총 N건)" 버튼을 추가했다(`handleSelectAllInFilter`/`listAllResultIds`). 클릭 시
  지금 탭·검색·정렬 조건으로 `ids_only=true`를 호출해 받은 id 전체를 **기존
  선택과 합집합**으로 더한다(다른 탭에서 이미 골라 둔 회사가 사라지지 않게).
  구버전 서버 응답(`ids: null`)은 `SelectAllUnsupportedError`로 잡아 선택을
  건드리지 않고 안내만 띄운다. 기존 `LARGE_SELECTION_HINT`(500건) 안내를
  그대로 재사용한다.

**두 다운로드의 컬럼 구성이 다르다(2026-07-28 변경)** — 같은 엔드포인트지만
**`ids` 유무 하나로만** 포맷이 갈린다. `include_history`는 `financial_history`
시트를 덧붙일 뿐 기본정보 포맷에 관여하지 않는다:

- **선택 항목 다운로드(`ids` 지정, 프론트 메뉴 3종 전부)**: 기본정보 15컬럼
  (결과ID/Job ID/고유번호/접수번호/회사명/주소/전화번호(미수집)/대표자명/
  업종코드/업종명/결산기준일/감사의견/감사인/감사인주소/감사인변동여부) +
  **"계정과목명"/"금액" long(세로) 포맷** + 맨 마지막 "파싱상태"(2026-07-28
  사용자 확정 — 계정과목명/금액보다도 뒤). 회사 1건이 당기(`_cur`) 계정과목
  19행으로 풀리고 기본정보는 각 행에 반복된다. **전기(`_prv`)는 싣지 않는다**
  (사용자 확정: "전기 항목은 전년도 당기 항목이니깐"). 값이 없는 계정과목도
  **행은 남기고 금액만 빈 값**으로 둔다(어떤 항목이 결측인지 보여야 하므로
  스킵 금지). 계정과목명은 wide 포맷 라벨에서 "(당기)"만 뗀 이름을 코드에서
  파생시켜(`SELECTION_ACCOUNT_LABELS`) 두 포맷의 라벨이 어긋날 여지를 없앴다
  (`SELECTION_ACCOUNT_COLUMNS`가 wide의 `_cur` 필드 전체와 일치하는지는
  `tests/test_exporters.py`의 드리프트 가드 테스트가 잠근다 — 새 `_cur`/`_prv`
  쌍을 추가하면 이 테스트가 먼저 깨진다). `ids`와 함께 `include_history=true`를
  주면 2시트 xlsx의 ① 시트가 이 long 포맷이다(② 시트는 아래 참고).
- **필터 전체 내보내기(`ids` 없는 export 경로)**: `include_history` 여부와
  **무관하게** 기존 wide 포맷(`RESULT_COLUMN_LABELS`, 회사 1행에 당기·전기 전
  항목) 그대로다 — 백엔드 로직은 **무변경**이지만, **화면 상단의 [Excel 다운로드]/
  [CSV 다운로드] 버튼은 2026-08-03에 제거해 이제 UI 진입점이 없다**(§4-11-A
  "현재 필터 전체 선택" + 선택 항목 다운로드로 같은 일을 할 수 있게 됐고, 사용자가
  "전기 금액은 안 나와도 된다"고 확인해 wide 포맷을 화면에 별도로 남길 이유가
  없어졌기 때문). API 자체는 다른 소비자를 위해 남겨 뒀다.

**② `financial_history` 시트도 long 포맷으로 전환(2026-07-29, 사용자 확정)**:
기존 "회사×회계연도" wide(재무 19항목이 각각 컬럼 + 감사인 + 파싱상태)에서
**"회사×회계연도×계정과목" 7컬럼**(결과ID/회사명/회계연도/접수번호/**재무제표명**/
계정과목/금액)으로 바꿨다. 스냅샷 1건이 재무 19항목만큼 19행으로 풀리고, 값이 없는
계정과목도 ① 시트와 같은 방침으로 **행은 남기고 금액만 빈 값**으로 둔다(금액 컬럼은
object dtype 고정 — float 승격 시 소수점/NaN이 찍힌다). 신설 "재무제표명"은
재무상태표(7)/손익계산서(8, 영업외수익·비용 포함)/현금흐름표(4) 중 하나이고,
**감사인·파싱상태 컬럼은 제거**했다. 행 순서는 result_id→회계연도 오름차순(호출부
정렬 그대로) → 그 안에서 재무상태표→손익계산서→현금흐름표다. 이 시트는 `ids` 유무와
무관하게 항상 같은 포맷이다(포맷 분기는 여전히 ① 시트에만 있다).

구현은 `app/exporters/excel.py`의 `SELECTION_EXPORT_COLUMN_LABELS`/
`results_to_selection_dataframe()`/`export_selection_results()`(① long)와
`FINANCIAL_SNAPSHOT_ACCOUNTS_BY_STATEMENT`/`FINANCIAL_SNAPSHOT_COLUMN_LABELS`/
`snapshots_to_dataframe()`(② long)이며, wide 포맷 함수(`RESULT_COLUMN_LABELS`/
`results_to_dataframe()`/`export_results()`)와 완전히 분리돼 있다. ② 시트의
계정과목 라벨·행 순서·재무제표 소속은 `FINANCIAL_SNAPSHOT_ACCOUNTS_BY_STATEMENT`
한 곳에서만 정의하고, 그 19항목이 ①의 `_cur` 19항목과 1:1로 대응하는지는
`tests/test_exporters.py`의 드리프트 가드 테스트가 잠근다(새 `_cur`/`_prv` 쌍을
추가하면 두 시트 모두에서 테스트가 먼저 깨진다). 2시트 xlsx를 만드는
`export_results_with_history()`는 `use_selection_format` 인자로 ① 시트만 골라
쓴다(시그니처 무변경) — 호출부가 `selected_ids is not None`을 그대로 넘긴다.

**선택 항목 보고서 생성(§4-12, 백엔드·프론트 모두 2026-08-03 완료 — 아래 마지막
항목 참고)**: 위 "선택 항목
다운로드" 옆에 붙는 **별개 기능**이다 — 다운로드가 데이터를 브라우저로
내려보내는 것과 달리, 이쪽은 **회사마다 감사 수임 제안서 HTML을 1개씩 만들어
로컬 폴더에 떨궈 놓고 그 경로를 응답으로 알려준다**(우편 발송용 라벨 엑셀 동봉).
`POST /api/jobs/{id}/generate-report`, body `{"ids":[1,2,3]}`(필수 1개 이상),
응답 `{output_dir, generated_count, files[], label_file, warnings[], skipped[]}`.

- 산출물: `<report_output_dir>/YYYY-MM-DD[_2,_3...]/{회사명}.html` +
  `발송처_목록.xlsx`(회사명/주소 2컬럼). 기준 폴더는 `Settings.report_output_dir`
  (기본 `BACKEND_DIR/report` — 개발 시 `backend/report`, exe 배포 시 exe 옆).
  **같은 날 재호출해도 기존 폴더를 덮어쓰지 않고 `_2`, `_3` 폴더를 새로 만든다.**
- 템플릿은 저장소 루트 `tamplate/audit_proposal_template.html`(폴더명 오타가
  실제 이름)이고, HTML/CSS/차트 로직은 **원칙적으로** 손대지 않고
  `const EMBEDDED_DATA = {...};` **블록만 통째로 교체**한다(중괄호 짝맞추기 스캐너 +
  `<`/U+2028 이스케이프). **exe 빌드 시 `--add-data "../tamplate;tamplate"`를
  빠뜨리면 507로 실패한다.** 이 "템플릿 무수정" 관행에는 **예외 5건**이 있다 —
  아래 "자본잠식 회사 등급"·"자본잠식 리스크 문구"·"자본잠식 차트 2종" 항목
  (앞의 셋은 원인이 같다: 자본총계가 음수면 부채비율도 음수가 되는데, 음수는 결측
  가드를 정상값으로 통과한다)과 "감사의견 태그 배경색 fallback"(④, CSS 1줄),
  "빈 비교군 안내 문구"(⑤, HTML 2줄 + CSS 4줄 — ④⑤는 JS 무변경).
- 재무 데이터는 **기존 재무이력 조회(`get_result_history`)를 그대로 재사용**하고
  새 쿼리를 만들지 않는다. DB 스키마 변경·추가 API 호출 0건이라 기존 완료 Job에서
  바로 쓸 수 있다. 사무소 소개 문구는 `app/reports/firm_profile.py`의
  `FIRM_PROFILE` 상수 한 곳에만 있다(문구 수정 시 이 파일만 고친다).
- **동종업종/지역 비교군(`peers`/`industryAverage`/`regionGroup`)은 "같은 Job에서
  이미 수집한 다른 회사들"로 채운다**(2026-08-04). 외부 API 호출·DB 스키마 변경 0건이라
  기존 완료 Job에서도 바로 나온다. 규칙은 `app/reports/audit_proposal.py`의 "비교군"
  섹션에 있고, 후보 풀은 `app/api/results.py::_load_job_peer_pool()`이 **요청당 한 번**
  (쿼리 2건: 결과 5컬럼 + 그 Job의 스냅샷 전체) 만들어 `generate_reports(peer_pool=...)`로
  넘긴다 — 선택 회사마다 조회하면 그대로 N+1이다(실측 Job 27: 후보 1,142건 118ms).
  - **후보 자격**: `excluded_by_stale_disclosure != 1`(휴면·폐업 추정 제외 — 화면 기본
    숨김과 같은 판단) **AND** `select_financial_rows()` 결과 1개년 이상(대상 회사의
    "쓸 수 있는 연도 0건이면 생성 스킵"과 같은 기준 재사용). `excluded_by_revenue`/
    `excluded_by_assets`는 **제외하지 않는다** — 검색 조건인 규모 범위를 벗어났을 뿐
    정상 영업 회사라 비교군으로는 유효하다. **AND 매출총이익률·영업이익률이 ±100%
    이내**(2026-08-04 추가, `MAX_COMPARISON_ABS_RATIO`) — 파싱은 맞는데(실측
    `(주)근하하이테크산업`: 매출 155백만/매출원가 3,035백만, `parse_status=OK`) 비율이
    **-1857.9%** 인 회사 한 건이 지역 순위 차트(`renderHBarChart`)의 축을 통째로 끌고 가
    **대상 회사 막대가 7px로 뭉개지고 나머지가 전부 100% 동률처럼** 보였다. 정렬 기준마다
    따로 거르지 않고 **후보 풀에서 한 번만** 걸러 peers/regionGroup에 같은 기준이 적용되게
    한다. 경계값(정확히 ±100%)은 포함하고, **대상 회사 자신에게는 적용하지 않는다**(대상은
    있는 그대로 실린다). 개발 DB 실측: 후보 1,522건 중 34건(2.2%) 제외.
  - 회사 1건이 비교군에 싣는 재무값은 **그 회사 자신의 최근 연도(`rows[-1]`) 하나**뿐이고,
    비율 4종은 템플릿 `calcRatios()`와 **글자 그대로 같은 식**으로 계산한다(드리프트 가드
    테스트가 템플릿 원문과 대조해 잠근다 — 식이 갈리면 같은 표 안에서 대상 회사는
    템플릿이, 비교군은 백엔드가 계산한 다른 기준의 숫자가 나란히 인쇄된다).
  - **업종 매칭은 `match_by_industry_prefix()` 한 함수에만 있고 peers/regionGroup이
    함께 쓴다**(2026-08-05): `induty_code` **소분류(앞 3자리) prefix** 우선, 매칭이 2건
    미만이면 **중분류(앞 2자리)** 로 한 번만 폴백(대분류까지 넓히지 않는다). 그래도 2건
    미만이면 빈 목록(에러가 아니라 정상). 실측 코드 길이가 2~5자리로 제각각이라 prefix
    매칭이 유일하게 안전한 방식이다. 규칙을 두 곳에 복사해 두면 한쪽만 고쳐져 **같은
    문서 안에서 "동종업종 표에는 있는데 지역 표에는 없는 회사"** 가 생기므로 함수를
    나누지 말 것. **최소 표본(2건) 판정은 호출부가 넘긴 목록 기준**이라, 각 비교군의
    자격 요건을 적용한 **뒤에** 이 함수를 부른다(나중에 걸러낼 회사를 세면 표본이
    충분한 줄 알고 폴백을 멈췄는데 정작 표에는 한 곳만 남는다).
    **[알려진 제약, 2026-08-05 L3 — 문서화만, 로직 무변경]** 규칙(함수)은 공유하지만
    **입력 목록이 달라**(peers는 의견거절/부적정을 먼저 뺀다) 소분류에서 표본 2건을
    채우는지가 갈릴 수 있고, 그러면 **선택되는 분류 단계(소분류 vs 중분류)가 두 표에서
    달라진다** — 결과적으로 "peers 차트에는 있는데 지역 표에는 없는 회사"가 남는다
    (실측 후보 1,488건 기준 단계 불일치 6건, 실제 피해 4건 = 0.27%. 업종 매칭 도입 전
    1,421건에서 대폭 줄었지만 0은 아니다).
  - **peers**: 위 업종 매칭 결과를 매출 근접순 8건으로 자른다. 표본이 부족하면
    `peers=[]`·`industryAverage=null`. **감사의견이 의견거절/부적정인 회사는 업종 매칭
    전에 후보에서 통째로 뺀다**(2026-08-04, 아래 항목 참고).
    **[알려진 제약, 2026-08-04 L4 — 로직으로 고치지 말 것]** 그 자릿수 편차(2자리 455건/
    3자리 356/4자리 942/5자리 3,451) 때문에 매칭이 **비대칭**이다 — 2자리 회사는 소분류
    단계를 건너뛰고 곧장 중분류 전체와 매칭돼 "A는 B를 동종으로 보는데 B는 A를 안 보는"
    조합이 생기고, "동종업종"의 해상도가 회사마다 다르다. 위 "세분류·세세분류 prefix
    조용한 누락"과 같은 성질의 구조적 제약이라 업종 트리 재설계 없이는 해소되지 않는다.
  - **industryAverage**: peers **만으로**(대상 회사 제외) `sum(매출총이익)/sum(매출액)`
    **매출액가중평균**. 비율끼리 산술평균하면 영세 회사 한 곳의 극단값에 끌려간다.
    `설명` 문구는 "…등 **매출 규모가 유사한** 동종업종 N개사 매출액가중평균(기준 연도)"
    이다(2026-08-04 문구만 수정, 계산값 무변경) — peers는 매출 근접순 상한 8건 절단을 이미
    거친 표본이라 "동일 업종 N개사"라고 쓰면 "그 업종에 N개사뿐"으로 오독된다.
    - 괄호 안 **기준 연도**(2026-08-04 L1, `build_comparison_year_note()`)는 비교군의
      연도가 회사마다 다를 수 있다는 사실을 문서에 남긴다 — 후보의 재무값은 각 회사
      자신의 "쓸 수 있는 최근 연도" 1개라, 개발 DB 실측 peer 짝 9,753건 중 602건(6.2%,
      Job별 1.6~12.3%)이 대상 회사와 회계연도가 다르다(표기가 없으면 "동일 시점 비교"로
      오독된다). 전부 같으면 `(2025년 기준)`, 다르면 `(각 사 최근 결산연도 기준 · 대상
      2024년 / 비교사 2023~2025년)`. **템플릿 무수정** — 733행 `peerFootnote`가 백엔드가
      준 `설명`을 그대로 이어 붙여 찍기 때문이다(peer 이름에 연도를 붙이면 차트 축
      라벨까지 길어져 기각). **regionGroup 표는 연도 컬럼이 없고 추가하려면 템플릿
      `<thead>`를 고쳐야 해 손대지 않았다** — 연도 표기는 peers 각주에만 있다.
  - **regionGroup**: 시도/시군구 세분화 없이 **같은 Job 전체**가 후보 풀이고(Job의 검색
    조건 자체가 지역이므로), 그 안에서 **업종까지 같은 회사만 싣는다**(2026-08-05 추가 —
    peers와 **같은** `match_by_industry_prefix()`. 그전에는 업종을 보지 않아 조선부품·
    시내버스운송·도장공사·상품중개가 한 문서의 표에 나란히 실렸고, 사용자가 실제 생성
    문서를 보고 지적해 바뀌었다). 대상 회사가 항상 배열 첫머리에 `isTarget: true`로
    들어가고, **동종업종** 후보가 2건 미만이면 `[]`(peers가 표본 부족일 때 빈 값을 주는
    것과 같은 기준 — 1개사만 놓고 "지역 내 순위"라고 인쇄하면 대외 문서로서 오해를 준다.
    개발 DB 실측 후보 1,488건 중 63건(4.2%)이 이 경우다 — 그때 문서에는 아래 **예외 ⑤**의
    안내 문구가 대신 인쇄된다).
    **단, 이 "2건" 게이트가 곧 "비교 막대 2개"는 아니다**(2026-08-05 L2 — 문서화만,
    로직 무변경): 의견거절/부적정 회사는 표본으로는 세지만 템플릿 735행이
    `filter(r=>r.매출총이익률!=null)`로 걸러내므로 **표본 2건을 채우고도 순위 차트에는
    비교사가 1곳만 그려질 수 있다**(실측 6건, 0.4%). 표에는 두 곳 다 남으므로(수치만 "-")
    차트와 표의 건수가 다른 것은 정상이다.
    필터 순서는 **① 자기 자신 제외 → ② 업종 매칭 → ③ 최소 표본(2건) → ④ 매출 근접순
    상한 절단**이다 — ②가 ③④보다 **먼저**여야 (a) 표본·폴백 판정이 실제로 표에 실릴 회사
    기준이 되고 (b) 상한 15칸이 매출만 가까운 타업종으로 채워져 업종 필터가 무력해지는 일이
    없다. 반대로 **의견거절/부적정은 여기서 빼지 않는다**(peers와 다른 점) — 행은 남기고
    수치만 감추는 방식이라 표본으로도 센다. **회사명은 실명 그대로 노출**한다(DART 공시
    기반 공개 정보).
    **감사의견이 비어 있으면 `"미상"`으로 채운다**(2026-08-04 L5, `UNKNOWN_OPINION_LABEL`)
    — 템플릿 751행이 `<span class="opinion-tag ${r.opinion}">${r.opinion}</span>`로 찍어
    빈 문자열이면 **글자 없는 빈 태그**가 표에 남는다(실측: 감사의견 공란 후보 5건이
    **76개 대상 문서**의 지역 표에 섞인다). 대상 회사에도 같은 규칙을 적용하되(한 표 안에서
    표기가 갈리지 않게) 문서 상단 `company.opinion`은 무변경이고, 수치도 감추지 않는다
    ("미상"은 의견거절/부적정과 달리 신뢰성 판정이 아니다). 다만 `.opinion-tag`에 적정/
    한정/의견거절 클래스만 배경색이 있어 **"미상"(과 기존 "부적정")은 흰 글자로 렌더돼
    사실상 안 보였다** — 도입 당시에는 템플릿 수정이 필요해 남겨 뒀고, 같은 날
    **F1(템플릿 무수정 관행의 예외 ④, CSS 1줄)** 로 해결됐다(아래 항목 참고).
  - **감사의견 의견거절/부적정은 비교 수치로 쓰지 않는다 — 단 적용 지점이 두 비교군에서
    다르다**(2026-08-04, `UNRELIABLE_OPINIONS`. **한정은 제외 대상이 아니다** — 실무상
    흔한 의견이라 빼면 표본만 줄고, "특정 사유를 제외하고는 공정하게 표시"라 수치를 쓸 수
    있다. 사용자 확정). 근거는 템플릿 250-252행의 자체 규약인데, 그 규약이 **regionGroup
    에서만 실제로 지켜진다**:
    - `regionGroup`은 템플릿이 `filter(r=>r.매출총이익률!=null)`로 걸러내므로 `_region_row()`
      가 **금액/비율만 `None`** 으로 둔다 → 순위 차트에서 빠지고 표에는 `fmtM`/`pct`가 "-"로
      찍는다(회사명·업종·감사의견은 그대로 남는다).
    - `peers`는 템플릿 `chartPeer` 렌더가 **null 체크 없이** `p.매출총이익률*100`을 곱해
      JS에서 `null*100 == 0` → **"0.0%"라는 틀린 값이 조용히 인쇄된다**(제외가 아니라 왜곡).
      그래서 필드 null이 아니라 `select_peers()`가 **후보에서 통째로** 뺀다 — 상한 8건 절단
      **보다 먼저** 걸러야 `industryAverage` 가중평균에도 안 섞인다.
    - **대상 회사에는 적용하지 않는다**(M2 극단비율 필터와 같은 원칙) — 대상의 재무수치는
      같은 문서의 KPI·추이 차트·등급에 이미 인쇄되므로 지역 표에서만 "-"로 가리면 한 장
      안에서 서술이 어긋나고, 순위 차트의 대상 막대 강조(`targetIdx`)까지 사라진다.
      개발 DB 실측: 후보 1,522건 중 73건(4.8%)이 의견거절/부적정.
  - **개수 상한**(우편 발송용 3페이지 문서라 무제한으로 넘기면 안 된다): peers 8건,
    regionGroup 대상 포함 16건. 자르는 기준은 **대상 회사와 매출액 차이(절대값)가 작은 순**.
  - **비교군에 섞인 PARTIAL도 경고로 알린다**(2026-08-04 L2,
    `collect_comparison_warnings()`): 후보의 최근 연도 스냅샷이 `parse_status=PARTIAL`이면
    `PeerCandidate.partial`로 표시하고, 대상 회사 **1건당 한 줄**로 "비교군에 부분
    파싱(PARTIAL) 연도 데이터가 포함된 회사가 N곳 있습니다: 회사명(연도) … 외 N곳"을
    남긴다(이름은 3건까지). **후보에서 빼지는 않는다** — PARTIAL은 "값이 없다"가 아니라
    "검수가 덜 됐다"이고, 빼면 좁은 표본이 더 줄어든다. 대상 회사 자신의 PARTIAL은 이미
    경고 대상이었는데 비교군 쪽만 조용했다(실측: PARTIAL 후보 8건이 149개 대상 문서에 섞임).
  - **`compute_report_ratios()`의 분모 가드는 비대칭이다**(2026-08-04 L3): 매출액/자산총계는
    결측·0·**음수** 모두 `None`, 자본총계는 **0만** 막고 음수는 그대로 계산한다
    (`RATIO_POSITIVE_DENOMINATOR_KEYS`와 같은 규칙 — 자본잠식 연도를 버리면 그 회사가
    보고서를 못 받는다). 이전 구현은 `not revenue or not equity or not assets`(truthy
    검사)라 **음수가 통과**했다 — 지금 호출부에서는 실 피해가 없었지만 이 저장소가
    자본잠식으로 세 번 겪은 바로 그 유형이라 명시적으로 막았다.
  - **부수 효과 — 수익성 등급이 업종평균 대비 상대평가로 바뀐다**: 템플릿
    `scoreProfitability()`에 `if(industryAvgGpm!=null) s += (매출총이익률-업종평균)*200`이
    원래 있었는데 지금까지는 항상 `null`이라 죽어 있었다. 개발 DB 3개 Job 1,522건으로
    전/후를 재계산하니 **550건(36.1%)의 수익성 등급이 이동**한다(C→D 156, B→A 138 …).
    템플릿은 손대지 않았고 의도된 동작이다 — 이미 만들어 둔 HTML 파일은 그대로이고,
    **새로 생성하는 보고서부터** 적용된다.
- `opinionSummary`(종합의견 자유 서술)는 자동 생성 대상이 아니라 계속 `""`다.
- **템플릿은 빈 배열/결측값에 안전하지 않아, 백엔드가 "완전한 연도만 싣는다"로
  막는다**(2026-08-03, dart-qa 리뷰 반영 — `select_financial_rows()`):
  - 렌더 메인이 `financials[length-1].매출액`을 무조건 읽어 **빈 배열이면
    `TypeError`로 렌더가 중단**되고, 회사명/주소까지만 찍힌 채 KPI·차트·등급은
    물론 **마지막 장의 사무소 소개·연락처까지 빈 문서**가 인쇄된다(연락처 없는
    제안서 발송 사고). → 실을 연도가 0건인 회사는 **생성하지 않고** `skipped`와
    `warnings`("…보고서를 생성하지 않았습니다")로 알린다. 발송처 라벨 엑셀에도
    **실제 생성된 회사만** 싣는다.
  - `calcRatios()`가 null-safe가 아니라 결측이면 `NaN%`/`Infinity%`가 인쇄되고
    등급 산정이 **데이터가 없을수록 A등급**이 나오는 방향으로 고장난다. → 비율
    계산에 쓰는 10항목(`RATIO_REQUIRED_KEYS`) 중 결측이 있거나 분모 4항목
    (매출액/자산총계/자본총계/유동부채)이 0인 연도는 통째로 제외하고 경고에
    남긴다. 이 두 건에 한해서는 템플릿 계산 로직 자체를 손대지 않았다(변경 범위
    최소화).
  - **분모가 음수인 연도도 같이 제외한다**(2026-08-03 추가,
    `RATIO_POSITIVE_DENOMINATOR_KEYS` = 매출액/자산총계/유동부채). 분모가 음수면
    비율의 **부호가 통째로 뒤집혀** 등급이 반대로 간다 — 실측 `result_id=6704`의
    2021년(매출액 -5,409백만원 / 영업이익 -1,706백만원)이 영업이익률 **+31.5%**,
    **수익성 A**로 인쇄됐고 같은 문서 KPI에는 음수 매출액이 함께 찍혀 모순됐다.
    **`자본총계`는 이 목록에 넣지 않는다**(0만 제외) — 자본잠식은 실재하는 정상
    파싱 결과라 연도를 버리면 아래 "자본잠식 회사 등급"의 방침이 무너진다(개발 DB
    실측: 넣었다면 스냅샷 469행이 사라지고 전 연도 자본잠식 회사 **66곳이 보고서
    미생성**으로 전환). 실제 제외 범위는 개발 DB 전체에서 스냅샷 2행뿐이고 그로
    인해 보고서가 사라지는 회사는 0곳이다.
- **자본잠식 회사 등급 — 템플릿 무수정 관행의 예외 ①**(2026-08-03, dart-qa
  2차 리뷰 H4, 사용자 승인): `scoreStability()`가 `부채비율 < 0.5`에 +30점을 주는데
  **완전자본잠식(자본총계 < 0)이면 부채비율(=부채총계/자본총계)이 음수**라 이 조건에
  걸려 가점을 받았다 — 자본이 전액 잠식된 회사가 **재무안정성 B(양호)~A(우수)**로
  인쇄됐고, 개발 DB 실측으로 해당 128건이 **전부** B 이상이었다. 음수는 null도 0도
  NaN/Infinity도 아니라 `select_financial_rows()`의 결측·0분모 가드를 정상값으로
  통과하므로 **백엔드로는 막을 수 없어** 템플릿 JS를 직접 고쳤다(`scoreStability()`
  맨 앞 `if(last.부채비율 < 0) return 10;` 한 줄 = D 확정, HTML/CSS·다른 등급 함수
  무변경). 전수 재계산 결과 등급이 바뀐 것은 음수 128건뿐이고 나머지 1,460건은
  그대로다. 회귀 테스트 2건이 `tests/test_reports.py`에 있다(템플릿 원문에서 가드를
  파싱해 임계값·순서를 검증 + Node `vm` 최소 DOM 스텁으로 생성 HTML을 실제 렌더해
  `재무안정성=D` 확인, Node 없으면 후자만 skip).
- **자본잠식 리스크 문구 — 템플릿 무수정 관행의 예외 ②**(2026-08-03, 위 ①과 같은
  뿌리, 사용자 확정): `buildRiskList()`의 `last.부채비율 < first.부채비율`(초록
  "개선" 문구) 비교도 같은 음수 값을 그대로 통과해, **자본잠식으로 악화된 회사에
  "부채비율이 903.1%에서 -336.8%로 지속 개선되어 재무안정성이 강화되었습니다"**가
  인쇄됐다(실사례 `results.id=6223`, 개발 DB 실측 81건). 같은 문서에 ①로 D등급이
  찍히므로 **한 장 안에서 등급과 서술이 모순**됐다. → 개선 판정 앞에
  `if(last.부채비율 < 0)` 분기를 두어 **red "…완전자본잠식 상태입니다…"** 항목을
  넣고, 개선은 `first.부채비율 >= 0 && last.부채비율 < first.부채비율`일 때만
  인정한다(시작 연도만 음수인 자본잠식 탈출 30건은 대소 비교가 뒤집히므로 어느
  문구도 안 붙인다 — 수정 전과 동작 동일). **`buildRiskList()`의 이 조건 하나만**
  고쳤고 문구 자체·다른 리스크 항목·등급 함수·HTML/CSS는 무변경이다. 전수 재판정
  결과 초록 문구가 사라진 것은 음수 81건뿐(990 → 909)이고 양수 구간은 그대로다.
  회귀 테스트 2건이 `tests/test_reports.py`에 추가돼 있다(36 → 38건).
- **자본잠식 차트 2종 — 템플릿 무수정 관행의 예외 ③**(2026-08-03, dart-qa 3차 리뷰
  Medium-1/Medium-2, 위 ①②와 같은 뿌리, 사용자 확정): 등급(D)과 리스크 문구(빨강
  자본잠식 경고)는 이미 맞춰 놨는데 **차트만 모순된 그림·숫자를 인쇄**하고 있었다.
  - `renderDoughnut()`(자본구성): `total = 유동부채+비유동부채+자본총계`라 자본총계가
    음수면 유동부채 조각의 `frac`이 1을 크게 넘어 원호가 여러 바퀴 겹쳐 그려지고,
    자본총계 조각은 음수 스윕이 되며, 범례에 **"유동부채 903.2% · 비유동부채 61.5% ·
    자본총계 -864.7%"** 가 인쇄됐다(실측 `result_id=8583`). → 음수 조각이 있거나
    `total<=0`이면 **도넛을 그리지 않고 조기 return**해 "자본총계가 마이너스(완전
    자본잠식)여서 구성비를 표시할 수 없습니다" 안내 + **각 항목 금액(백만원)** 범례로
    대체한다(구성비는 음수 항목이 있으면 정의 자체가 성립하지 않는다).
  - `renderLineChart()`(재무안정성 추이): y축이 `0~maxV` 고정이라 음수 부채비율 지점이
    plot 밖으로 나가 **통째로 잘려 안 보였다**(8583은 8개 점 중 4개가 cy 592.7/578.6/
    528.5/498.8로 영역 14~164 밖). → `minV`까지 포함해 스케일을 잡고
    `yOf(v)=padT+plotH*(maxV-v)/span` + 0선을 긋는다(같은 템플릿
    `renderGroupedBarChart`의 `minV`/`zeroY` 패턴과 통일). **값이 전부 양수면 `minV=0`
    이라 수식이 기존과 항등**이라 렌더가 완전히 같다.
  - 실측 전/후(Node `vm` 렌더): 8583의 도넛 path 3개→**0개**, 범례가 구성비→금액,
    선그래프 plot 밖 점 4개→**0개**(y축 `0%~50%`→`50%~-200%`), 재무안정성 등급은 D로
    무변경. 회귀 테스트 3건이 `tests/test_reports.py`에 추가돼 있다(39 → 42건;
    Medium-3 백엔드 테스트 3건까지 45건).
- **감사의견 태그 배경색 fallback — 템플릿 무수정 관행의 예외 ④**(2026-08-04,
  dart-qa F1, 사용자 확정. **앞의 ①②③과 달리 자본잠식과 무관한 순수 CSS 건이다**):
  지역 비교표의 `.opinion-tag` 기본 규칙에 배경색이 없고 `color:#fff`만 있어,
  전용 클래스가 있는 `적정`/`한정`/`의견거절` 외의 값은 **흰 종이 위에 흰 글자**로
  찍혀 인쇄물에서 사실상 안 보였다(대상 회사 행은 베이지 `tr.target`이라 마찬가지).
  해당 값은 두 가지다 — 기존부터 있던 `부적정`(선행 결함)과, 비교군 기능이 새로
  채우는 `미상`(감사의견 결측 표시, 위 L5). **개발 DB 실측 143건(4.7%)**.
  → `.opinion-tag`에 `background:var(--gray);`(#5a6472, 팔레트에 이미 있는 회색)
  **한 줄만** 추가했다. 3개 클래스 규칙(`.opinion-tag.적정` 등)이 specificity로
  이 fallback을 그대로 덮어써 **기존 색은 무변경**이고, HTML 구조·JS 렌더 로직·다른
  CSS는 전혀 손대지 않았다. 회귀 테스트 1건이 `tests/test_reports.py`에 있다
  (Node `vm`으로 5종 의견을 실제 렌더한 뒤 태그 class를 템플릿 CSS로 풀어, 3종은
  팔레트 색 그대로 · `부적정`/`미상`은 배경이 있고 흰 글자 대비가 충분한지 확인).
- **빈 비교군 안내 문구 — 템플릿 무수정 관행의 예외 ⑤**(2026-08-05, dart-qa 리뷰 후속
  L1, 사용자 승인. ④와 마찬가지로 자본잠식과 무관하고 **JS는 손대지 않았다**):
  업종 매칭(2026-08-05)으로 `regionGroup=[]`이 되는 회사가 63건(4.2%, Job별 최대
  20.2%) 생겼는데, **regionGroup이 비면 peers도 반드시 빈다**(지역 후보 목록이 peers
  후보의 상위집합이라 논리적으로 필연 — 실측 63건 전부 해당). 그 결과 2페이지의 비교
  영역 3개("동종업종 비교" 차트 / "동일 지역·규모 비교군 순위" 차트 / "비교군 상세
  현황" 표)가 **안내 한 줄 없이 통째로 백지**로 인쇄됐다. peers 쪽은 각주가 이미
  "동종업종 비교사 데이터 없음"을 찍는데 지역 쪽 두 섹션만 조용했던 것이 차이다.
  빈 목록이 **맞는 답**이라 백엔드 데이터로는 고칠 수 없어 템플릿을 고쳤다.
  → `.empty-note` 안내 div **2개**(순위 차트 자리 / 상세 표 자리)를 HTML에 두고,
  `#page2:has(#regionTable tbody:empty)`(= JS가 그린 행이 0건)를 조건으로 안내를
  보이고 빈 차트 카드(`#regionRankCard`)·머리글만 남은 표(`#regionTable`)·의견거절
  각주(`#regionOpinionNote`)를 숨기는 **CSS 4줄**만 추가했다. **JS 렌더 로직·문구·
  다른 섹션은 전혀 무변경**이라 regionGroup이 있는 문서(95.8%)는 렌더 결과가 한 글자도
  바뀌지 않는다(`:has()` 미지원 브라우저에서는 안내가 안 뜰 뿐 기존 동작 그대로 —
  degrade만 하고 깨지지 않는다). 회귀 테스트 2건이 `tests/test_reports.py`에 있다
  (템플릿 CSS를 파싱해 두 상태의 `display`를 풀고 JS에 신설 토큰이 없는지 확인 +
  Node `vm`으로 비교군 없는 회사·있는 회사를 한 번에 렌더해 표 0행/3행·막대 0개/3개
  대조). 실제 브라우저(Chrome·Edge headless) 실측으로도 빈 문서에서 안내가
  `display:block`, 차트/표/각주가 `none`이고, 비교군이 있는 문서는 `block`/`table`/
  `block` + 안내 `none`으로 기존과 동일함을 확인했다.
- **검수 상태를 대외 문서 단계에서도 끊지 않는다**(2026-08-03): `parse_status`
  FAILED 연도는 제외, PARTIAL 연도와 전기 유래 연도(`from_current_period=0`)는
  싣되 경고로 알린다. `results.parse_status`가 PARTIAL/FAILED이면 회사 단위
  경고도 추가한다. `FIRM_PROFILE["contact"]`가 자리표시자 그대로면 요청당 1건
  경고(`is_placeholder_contact()`).
- 파일 저장 실패는 500이 아니라 **507 + 한국어 메시지**다(라벨 엑셀의 openpyxl
  `IllegalCharacterError`도 507로 통일 — 회사명/주소는 쓰기 전 제어문자 제거).
  **프론트엔드도 구현 완료**(2026-08-03) — 선택 표시줄의 "선택 항목 보고서 생성"
  버튼(`handleGenerateReport`)이 `POST /generate-report`를 호출하고, 결과(저장
  경로·생성 건수·경고)는 `frontend/src/components/ReportResultModal.tsx`가
  모달로 보여준다. 이 문서는 한동안 "미구현"으로 잘못 남아 있었다 — 실제 코드가
  이미 구현돼 있었는데 문서만 갱신이 안 된 drift였다(2026-08-03 dart-qa 리뷰로
  발견).

**보고서 생성 전 확인 모달의 선택 요약(§4-12-A, 백엔드·프론트 모두 2026-08-03 완료)**:
`POST /api/jobs/{id}/results/selection-summary`, body `{"ids":[1,2,3]}`,
응답 `{total, stale_disclosure, excluded_revenue, excluded_assets, failed,
no_disclosure, no_history}`(전부 건수 — 뒤의 두 필드는 2026-08-04 추가, 아래
"필드 의미" 항목 참고). "현재 필터 전체 선택"(§4-11-A)으로 수천 건이 선택된 채 확인 없이
제안서가 쏟아지고, 탭을 넘나든 **합집합 선택**에 조건 불일치 회사(휴면·폐업 추정/
매출액·총자산 제외/파싱 실패)가 섞여 우편 발송될 수 있다는 지적에서 나왔다.

- **필터 조건이 아니라 명시적 id 목록에 대한 집계**라, `ids_only`처럼 `GET /results`에
  옵션을 얹지 않고 **별도 POST 엔드포인트**로 뒀다(판단 근거는 상세개발계획.md
  §4-12-A — 목록 조회에 "필터를 무시하는" 두 번째 경로를 만들지 않기 위함 + 응답
  스키마가 `ResultListResponse`와 다름 + 수천 건 id는 URL 길이에 매이면 안 됨).
- **입력·검증·에러 계약이 `POST /generate-report`와 100% 동일**하다(같은
  `_validate_report_ids()` + 이번에 공용화한 `_assert_ids_belong_to_job()`): 빈 목록
  422, 중복 제거, 범위 밖 정수 400, **타 Job의 id 400(문구까지 동일)**, 없는 Job 404.
  요약이 통과시킨 선택을 생성이 400으로 막으면 확인 모달이 거짓 안내가 되기 때문이다.
- 한 회사가 매출액·총자산 두 조건에 **동시에** 걸릴 수 있어(두 필터는 독립 판정)
  각 건수의 합이 `total`을 넘을 수 있다 — 화면에서 더해 쓰면 안 된다. 플래그는
  `== 1`로만 센다(기존 Job 행은 NULL/0일 수 있고 둘 다 "해당 없음").
- **필드 의미 — `failed`/`no_disclosure`/`no_history`는 서로 다른 축이다**
  (2026-08-04, dart-qa 실측 지적 반영. 최초 구현은 `failed`가 FAILED 전부를 셌다):
  - `failed` = 화면 **"파싱 실패(검수 필요)" 탭과 동일한 조건**
    (`parse_status='FAILED'` **AND** `rcept_no IS NOT NULL`). Phase 2 B1이 공시를
    못 찾은 건에도 FAILED를 쓰기 때문에(`pipeline.py`) FAILED 전체를 세면 검수
    필요 건수가 수천 배 부풀려진다 — **개발 DB 실측: FAILED 2,215건 중 2,214건
    (99.95%)이 `rcept_no IS NULL`이고 진짜 검수 필요는 1건**인데, 모달은 "파싱
    실패 1,871건"이라고 띄우고 있었다.
  - `no_disclosure` = 화면 **"감사보고서 없음" 탭과 동일**(`rcept_no IS NULL`).
    검수 대상이 아니므로 경고가 아니라 안내용이다.
  - `no_history` = `financial_snapshots`가 0건인 회사 수. §4-12 생성 로직이 이런
    회사를 **건너뛰므로**, 확인 모달의 "N건 생성"은 `total`이 아니라
    `total - no_history`(=**최대** 생성 가능 건수) 기준이어야 한다(실측 Job 27:
    4,383건 중 3,149건이 스냅샷 0건 → 실제 산출물 최대 1,234건). 이력이 있어도
    전 연도가 파싱 실패/결측이면 추가로 건너뛰므로 어디까지나 상한이다.
- 읽기 전용이고 DB 스키마 변경·추가 API 호출 0건이라 기존 완료 Job에서 바로 쓸 수
  있다. `no_history`는 스냅샷 `result_id`를 `DISTINCT`로 한 번 더 조회해 판정한다
  (회사마다 `get_result_history()`를 부르지 않는다 — 필요한 건 "0건인가"뿐).
- **확인 모달 UI도 구현 완료(2026-08-03)**: `frontend/src/components/ReportConfirmModal.tsx`
  — 생성 **후** 결과를 보여주는 `ReportResultModal.tsx`와 **별개 파일**이다(이쪽이 앞
  단계). "선택 항목 보고서 생성" 버튼은 이제 곧바로 생성하지 않고 이 모달을 열며,
  모달이 열릴 때 `getSelectionSummary()`(`api/results.ts`)를 호출한다.
  `handleGenerateReport()`/`generateReport()` 본체는 **무변경**이고 그 앞 단계로만
  끼워 넣었다.
  - 경고 4종(`stale_disclosure`/`excluded_revenue`/`excluded_assets`/`failed`)은
    노란 Alert 안에 **각각 한 줄씩** 나열하고(0인 항목 생략) **절대 합산하지 않는다** —
    "한 회사가 여러 항목에 동시에 해당할 수 있어 합이 선택 건수와 맞지 않을 수 있다"는
    문구를 함께 둔다.
  - **2026-08-04 백엔드 필드 보정 반영 완료(dart-frontend)**:
    - `failed` 줄의 "감사보고서 없음도 포함됩니다" 보조 문구를 제거하고 "원문 파싱에
      실패해 검수가 필요한 회사"로 바꿨다(이제 사실이 아니므로).
    - `no_disclosure`는 **경고 목록에 넣지 않는다** — 사용자가 고칠 것도 검수할 것도
      없는 사실 안내라, 노란 경고 Alert 아래에 **파란(`color="blue" variant="light"`)
      별도 Alert**로 톤을 낮춰 분리했다("검수 대상은 아니며, 위 파싱 실패 건수와도
      겹치지 않습니다" + "대부분 재무 이력도 없어 건너뛰어집니다"). 0건이면 표시 안 함.
      경고가 하나도 없을 때 뜨는 초록 Alert 문구는 경고 4종만 열거한다(감사보고서
      없음은 그 판정에 관여하지 않으므로).
    - 확인 버튼 라벨은 `no_history > 0`이면 "**최대** N건 생성"(N = `total -
      no_history`), 0이면 기존 "N건 생성"이다. 같은 내용을 본문에도 한 줄로 안내한다
      (선택 T건 중 H건은 재무 이력이 없어 파일이 안 만들어짐 → 최대 N건). **넘기는
      id 목록은 여전히 선택 전체**다 — 어떤 회사가 스킵될지는 생성 시점에 백엔드가
      판단하므로 프론트가 미리 걸러내지 않는다(문구는 기대치 보정용일 뿐).
  - **경고가 있어도 생성을 막지 않는다**(사용자가 알고 진행하게만 한다). 반대로
    **요약 조회가 실패하면 생성 버튼을 잠근다** — 무엇이 만들어질지 모른 채 수천 건을
    만들지 않기 위함.
  - 저장 위치는 폴더 규칙(날짜 폴더 / 같은 날 재생성 시 `_2`,`_3` / 브라우저 다운로드가
    아니라 프로그램이 도는 PC의 폴더)만 안내하고 **절대 경로는 지어내지 않는다** —
    `Settings.report_output_dir`를 노출하는 조회 API가 없어 화면이 알 수 없다. 실제
    경로는 생성 후 `ReportResultModal`이 응답의 `output_dir`로 보여준다.
  - 생성 중에는 확인 모달이 닫히지 않는다(닫기/Esc/바깥 클릭 비활성, 확인 버튼 loading)
    — 중복 클릭으로 `_2` 폴더가 생기는 것을 막는다.

**결과 목록 "파싱비고" 컬럼(2026-08-03)**: `results.parse_note`(파서가 남긴 검수 사유)를
화면 컬럼으로 추가했다(`util/resultColumns.ts::STATUS_COLUMNS`). 상단 wide 다운로드
버튼을 없앤 뒤로 검수 사유를 볼 경로가 개별 회사 상세 Drawer 하나뿐이었기 때문이다.
기본 숨김 + 컬럼 토글로 켜되, **"파싱 실패 (검수 필요)"/"부분 성공" 탭에서는
`forcedVisibleKeys`로 항상 표시**한다("휴면·폐업 추정" 탭의 최근 공시일자와 같은 방식).
백엔드 `SORTABLE_COLUMNS`에 없는 컬럼이라 `sortKey: false`(헤더 클릭 비활성)이고,
셀은 2줄로 잘라 전체 문구는 툴팁으로 보여준다. **다운로드 포맷(long/wide)은 무변경**이다.

### "최근 1년 이내 DART 공시 없음" 배제 (2026-07-21 추가)

실사례("주식회사 유진"류 — 폐업/휴면/합병소멸 등으로 실질적으로 활동을 멈춘
법인)를 걸러내기 위한 필터. `excluded_by_revenue`/`excluded_by_assets`와 같은
패턴이다 — Phase 2 **B2**가 최신 rcept_no를 찾으려고 이미 호출하는 `list.json`
응답을 재사용해 판정한다(**추가 API 호출 0건**). rcept_no 앞 8자리(접수일자)를
`results.latest_disclosure_date`에 남기고, 365일보다 오래됐거나 공시 자체가
없으면 `results.excluded_by_stale_disclosure=1`로 표시하는 순수 사후 필터
플래그다. `GET /results`/`/export`가 동일한 tri-state 쿼리 파라미터로
필터링을 지원한다. **소급 반영 완료(2026-07-22)** — 기존 완료 Job 1,211행 중
204행(16.8%)이 0→1로 전환됨(경위·검증은 개발이력.md 참고). **프론트엔드
미반영**: 결과 화면에 새 필터 탭 추가와 기본 노출 여부 결정은 dart-frontend
몫으로 남아 있다.

### 알려진 구조적 제약 (변하지 않음)

1. OpenDART에는 지역 검색이 없다 — 그래서 위 Phase 1/2 아키텍처가 필요하다.
2. 비상장 외감법인은 DART 재무제표 API를 지원하지 않는다 — 감사보고서
   원문을 직접 파싱해야 한다.
3. 매출액/총자산은 원문을 열기 전엔 알 수 없어 구조적으로 사후 필터다.
4. DART 일일 호출 한도 20,000건 — Job은 `PAUSED_QUOTA`로 자동 전환,
   체크포인트로 resume.
5. 파싱은 100% 자동화되지 않는다 — `parse_status`로 검수 대상을 남긴다.

작업을 시작하기 전에 반드시 아래 두 문서를 먼저 읽으세요 —
이 저장소의 유일한 진실 소스(source of truth)입니다.

- [PRD.md](PRD.md) — 제품 요구사항: 무엇을, 왜 만드는지, 확보 가능한 데이터 항목, 리스크
- [상세개발계획.md](상세개발계획.md) — 위 PRD를 웹앱으로 구현하기 위한 기술 설계:
  아키텍처, DB 스키마, API 설계, 파이프라인 단계, 마일스톤

코드를 작성하기 시작하면, 실제 구현이 두 문서와 달라지는 지점(설계 변경, 스파이크 결과 등)이
생길 수 있습니다. 그런 경우 이 CLAUDE.md와 상세개발계획.md를 함께 갱신해 다음 세션이
최신 상태를 참고할 수 있게 하세요.

## 제품 개요

지역 / 매출액 범위 / 업종 조건을 입력하면 OpenDART API 기반으로 **외부감사대상 비상장
법인**의 기본정보 + 요약 재무정보(당기·전기)를 자동 수집하는 도구. 세무회계사무소가
신규 거래처를 발굴하기 위한 용도이며, **데이터 수집기 + 결과 조회 웹앱까지가 이
프로젝트의 전체 범위다** (2026-07-17, 전단지/진단자료 생성 등 활용 단계는 범위에서
제외하기로 확정 — 경위는 [개발이력.md](개발이력.md)의 "Phase 2(전단지 생성) 범위
제외 확정" 참고).

## 핵심 아키텍처 (M8 재설계 후 현재 구현 기준)

```
브라우저 (React SPA)
  └─ REST API (폴링 방식 진행률 조회)
      FastAPI 서버 ─── SQLite (dart_corp_index / fsc_financial_stat / jobs / results / financial_snapshots)
        └─ 백그라운드 워커: Phase 1(후보 확정, 로컬 쿼리만) → Phase 2(DART 원문 크롤링·파싱)
            └─ OpenDART API (corpCode / list / document) — Phase 2에서만 호출
```

- 백엔드: Python 3.12 + FastAPI, 수집 작업은 `BackgroundTasks` 기반 Job으로 실행
  (수 분~수 시간 소요, 진행률은 프론트가 폴링)
- DB: SQLite (SQLAlchemy) — 단일 파일, 배포 시 PostgreSQL 전환 가능하게 설계
- 프론트: React 18 + Vite + TypeScript (Mantine)
- HTTP: `httpx` 비동기 (OpenDART 병렬 호출, 타임아웃/재시도)
- 재무제표 파싱: XML 1순위(`lxml`) → PDF 2순위(`pdfplumber`) → HWP는 실패 기록만
- 디렉터리 구조와 각 모듈의 책임은 [상세개발계획.md §3](상세개발계획.md)의 트리를 참고할 것
  (최초 계획 트리 기준이며, M6~M8에서 추가된 모듈은 아래 "프로젝트 현황" 요약과
  [개발이력.md](개발이력.md) 참고)

### 왜 이렇게 설계되었는가 — 반드시 알아야 할 구조적 제약

1. **OpenDART에는 지역 검색 파라미터가 없다.** 회사 주소는 기업개황(company.json)을
   회사별로 1건씩 조회해야만 알 수 있어, "김해만 검색"해도 후보 전체(연간 약 3~4만 개사)의
   기업개황이 필요하다 — 이것이 Phase 1/Phase 2 분리 아키텍처의 근본 이유다.
   **최초 계획은 공공데이터포털 금융위 API로 이를 우회하는 것(대응 1/대응 2)이었으나,
   M8 재설계로 폐기하고 DART corpCode 전수 인덱스(`dart_corp_index`) 자체를 로컬에
   구축해 지역/업종/상장여부를 API 호출 없이 쿼리하는 방식으로 대체했다** — 금융위
   API 기반 방식은 최신성이 1년까지 뒤처져 조건에 맞는 회사를 조용히 놓치거나(사전
   재무 스크리닝) 이름 매칭 오류로 동명이인 회사를 잘못 연결하는(corp_code 추정)
   문제가 실측으로 확인됐기 때문이다. 상세 경위는 [개발이력.md](개발이력.md)의
   "M8" 관련 기록 참고, 최신 설계는 [상세개발계획.md §4-7~§4-10](상세개발계획.md) 참고.
2. **비상장 외감법인은 재무제표 API(fnlttSinglAcntAll 등)를 지원하지 않는다.** 상장법인/IFRS
   사업보고서 제출대상만 지원되므로, 재무정보는 **감사보고서 원문(document.xml)을
   다운로드해 직접 파싱**해야 한다. 이것이 파이프라인에서 가장 리스크가 큰 구간(M3,
   [상세개발계획.md §4-4](상세개발계획.md))이다.
3. **매출액/총자산은 구조적으로 사후 필터일 수밖에 없다.** 원문을 파싱하기 전에는 확정치를
   알 수 없기 때문. 금융위 API 참고값(`ref_*`)으로 Phase 2 처리 순서만 정할 뿐, 포함/제외
   판정은 항상 원문 파싱 이후에만 이뤄진다(위 "프로젝트 현황" 요약 참고).
4. **일일 API 호출 한도 20,000건.** `dart_client.py`가 호출 카운터를 내장해 상한 도달 시
   Job을 `PAUSED_QUOTA`로 자동 전환, 다음 날 재개 가능해야 한다. 각 단계는 DB에 체크포인트를
   남겨 **중단 후 이어하기(resume)**가 가능해야 함.
5. **파싱은 100% 자동화되지 않는다.** 회사마다 원문 서식이 달라 `parse_status`
   (OK/PARTIAL/FAILED)를 결과마다 남기고, 화면에서 검수 필요 건을 필터링해 재시도할 수
   있게 한다.

### DB 스키마 핵심 테이블

최초 계획(§5)의 기본 골격 + M6~M8에서 추가된 핵심 테이블:

- `dart_corp_index`: DART corpCode 전수 인덱스(`corp_code` PK) — Phase 1 지역/업종/
  상장여부 필터의 유일한 데이터 소스(M8, 외부 API 호출 0건)
- `fsc_financial_stat`: 금융위 API 매출액/총자산 참고값 스냅샷 — Phase 2 처리 순서
  결정에만 쓰이고 판정에는 쓰이지 않음(M8)
- `jobs`: 검색 조건 + 진행 상태(`PENDING/RUNNING/PAUSED_QUOTA/DONE/FAILED/CANCELLED`)
  + `phase`(`CANDIDATES`/`FINANCIALS`)
- `results`: 회사 1건 = 1행, 기본정보 + 당기(`_cur`)/전기(`_prv`) 재무 항목(현금흐름표
  포함) + `parse_status` + 참고값(`ref_*`) + `excluded_by_*` 필터 플래그
- `financial_snapshots`: 회사×회계연도 단위 다년치 재무 이력(M2 STEP 7) +
  그 연도를 당기로 감사한 `auditor_name`(2026-07-26, `results.auditor_changed`의 원천)
- `corp_cache`: corpCode.xml 전체 고유번호 목록 캐시 (레거시, `dart_corp_index`와 역할 일부 중복)
- `api_usage`: 일일 호출량 카운터

전체 컬럼 정의는 [상세개발계획.md §5](상세개발계획.md) 참고. `fsc_corp_index`
테이블은 2026-07-21 삭제됐다 — `fsc_financial_stat`과 이름이 비슷하지만 별개였고
혼동하지 말 것(상세는 [개발이력.md](개발이력.md) 참고).

## 개발 시작 시 참고사항

- API 키(OpenDART, 공공데이터포털)는 `.env` + `pydantic-settings`로 관리, 코드 하드코딩 금지.
  프론트에는 절대 노출하지 않고 모든 DART 호출은 백엔드 경유.
- 마일스톤 M1~M8은 모두 완료된 상태다(위 "프로젝트 현황" 참고) — 이 저장소에서 새로
  작업할 때는 그 최종 상태를 기준으로 삼을 것. M1 시절 계획이었던 "금융위 API 커버리지
  스파이크로 대응 1 vs 2를 결정" 같은 초기 판단 과정은 M8 재설계로 이미 대체됐다(위
  "핵심 아키텍처" §1 참고) — 지금 시점에 다시 수행할 필요는 없다.
- `backend/tests/fixtures/`에 실제 감사보고서 원문 샘플(현재 20건 이상)과
  `manifest.json`이 있고, 파서 단위 테스트가 이를 근거로 작성돼 있다. 새 원문 서식
  변형을 발견하면 이 fixtures에 추가하는 기존 관행을 따를 것.

### 백엔드 실행/테스트 명령 (M1 스캐폴딩 완료 후 실제 확인된 명령)

```
cd backend
python -m venv .venv            # Python 3.11 또는 3.12 권장 (아래 "Python 버전 주의" 참고)
source .venv/Scripts/activate   # Windows Git Bash 기준. PowerShell은 .venv\Scripts\Activate.ps1
pip install -r requirements.txt

cp .env.example .env            # 실제 키 발급 후 값 채워넣기 (커밋 금지)

uvicorn app.main:app --reload   # http://127.0.0.1:8000, 기동 시 SQLite 테이블 자동 생성
pytest tests/ -q                # 2026-08-05 기준 463 passed
```

**Python 버전 주의**: 이 개발 환경의 기본 `python`이 3.14였는데, 3.14용 `pandas`/`lxml` 등의
사전 빌드 wheel이 아직 없어 `pip install -r requirements.txt`가 Meson/C 빌드 단계에서 실패했다
(Visual Studio 빌드 도구 필요). **Python 3.11 또는 3.12로 가상환경을 만들 것** — 이 저장소는
`py -3.11 -m venv .venv`로 확인 완료.

### 프론트엔드 실행 명령

```
cd frontend
npm install
npm run dev     # Vite dev 서버, /api는 vite.config.ts의 proxy로 백엔드(port 8000)에 전달
npm run build   # tsc 타입체크 포함
npm run lint    # oxlint
```

### 배포용 실행파일(.exe) 빌드 (2026-07-23 추가, 2026-07-23 배포 위치 분리)

Python/Node가 설치되지 않은 사무실 PC에도 배포할 수 있도록, PyInstaller로
백엔드(FastAPI+uvicorn)와 프론트엔드 빌드 산출물을 단일 exe로 묶는다.
API 키는 exe에 하드코딩하지 않는다 — `backend/launcher.py`가 exe 옆에
`.env`가 없으면 `.env.example`을 복사해 메모장으로 열고 안내창을 띄운 뒤
종료하며, 사용자가 키를 채워 저장하고 재실행하면 정상 구동된다(최초 1회).

**배포 산출물은 개발 저장소(`c:\claude\dart-search`)와 분리해 `C:\claude\dart-search-배포`에
둔다** (2026-07-23, 사용자 명시 요청 — 개발용 소스/DB와 실제 사무실에서 쓰는
배포본을 물리적으로 구분하기 위함). `--distpath`로 그 경로를 직접 지정해
빌드 결과가 바로 그리로 나가게 한다.

```
cd frontend && npm run build          # frontend/dist 생성 (exe에 번들됨)
cd backend && source .venv/Scripts/activate
pip install pyinstaller
pyinstaller --noconfirm --onefile --name dart-search \
  --distpath "C:/claude/dart-search-배포" \
  --add-data "../frontend/dist;frontend_dist" \
  --add-data ".env.example;." \
  --add-data "../tamplate;tamplate" \
  --collect-all fastapi --collect-all starlette --collect-all uvicorn \
  --collect-all pydantic --collect-all pydantic_core --collect-all sqlalchemy \
  --collect-all lxml --collect-all pdfplumber --collect-all pandas \
  --collect-all openpyxl --collect-all multipart --collect-all olefile \
  --collect-all httpx --collect-all anyio \
  --hidden-import app.api.jobs --hidden-import app.api.meta --hidden-import app.api.results \
  launcher.py
# 결과물: C:\claude\dart-search-배포\dart-search.exe (약 83MB)
# (재빌드해도 같은 폴더의 기존 .env/dart_search.db/data는 그대로 남고 exe만 갱신됨 —
#  onefile 모드는 exe 하나만 --distpath에 쓰고 나머지는 손대지 않기 때문)
```

구조: `app/config.py`의 `BACKEND_DIR`은 환경변수 `DART_SEARCH_APP_DIR`(exe가
놓인 폴더)을 우선 사용하도록 분기돼 있어, DB(`dart_search.db`)/캐시/`.env`가
exe 옆에 영구히 남는다(PyInstaller onefile의 임시 압축해제 폴더가 아님).
`app/main.py`는 `DART_SEARCH_RESOURCE_DIR`(PyInstaller 번들 임시폴더,
`sys._MEIPASS`) 아래 `frontend_dist`가 있으면 `/assets`를 정적 서빙하고
나머지 모든 경로를 `index.html`로 폴백하는 catch-all 라우트를 API 라우터들
**뒤에** 등록해 React Router(BrowserRouter) 클라이언트 라우팅을 지원한다 —
두 환경변수 모두 없으면(=일반 소스 실행) 기존 동작과 100% 동일하다.
`launcher.py`는 `DART_SEARCH_PORT`(기본 8000)로 uvicorn을 띄우고, 이미 그
포트가 쓰이는 중이면(중복 실행) 새 서버를 띄우지 않고 브라우저 창만 새로
연다. `backend/build/`·`backend/*.spec`은 PyInstaller 중간 산출물이라
`.gitignore`에 등록돼 있다(커밋 대상 아님) — 최종 배포본(`backend/dist/`가
아니라 `C:\claude\dart-search-배포\`)은 저장소 바깥이라 애초에 git 추적
대상이 아니다.
